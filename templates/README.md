# templates

Reserved in the original layout for template packages. Templates currently live in the application: tokens in `apps/platform/src/design/templates.ts`, layout in the `[data-template]` sections of `apps/platform/src/app/(sites)/s/[site]/site.css`. Rules: [`../docs/11-design-contract.md`](../docs/11-design-contract.md); designer brief: [`../docs/14-designer-brief.md`](../docs/14-designer-brief.md). They move here as versioned packages if and when templates need their own release cycle.
